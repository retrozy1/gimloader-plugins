{
  "name": "gimloader-communication",
  "version": "1.0.0",
  "description": "Send and listen for arbitrary messages between different clients in 2D gamemodes",
  "type": "module",
  "scripts": {
    "build": "npm run build:script && node run build:types",
    "serve": "gl serve",
    "build:script": "gl build",
    "build:types": "tsc",
    "prepare": ""
  },
  "author": "retrozy",
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "files": ["./lib"],
  "types": "./lib/index.d.ts"
}