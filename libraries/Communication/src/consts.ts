export enum Ops {
  TransmittingByteInteger,
  TransmittingBoolean,
  TransmittingNumber,
  TransmittingString,
  TransmittingObject,
}

//Declaring which don't have aiming rather than which is not
export const gamemodeIdsWithoutAiming = [
  'dontLookDown'
];